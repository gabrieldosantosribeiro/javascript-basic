/*
Create a variable called text that contains a sentence. Use the replace() method to replace a word in the sentence and display the result.
*/

let text = 'the green color will be replace'
let newtext = text.replace('green', 'blue')
console.log(newtext)